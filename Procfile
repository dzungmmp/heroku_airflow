# Step 1
web: DATABASE_URL=${DATABASE_URL/postgres/postgresql} airflow db init
# Step 2
# web: DATABASE_URL=${DATABASE_URL/postgres/postgresql} airflow webserver --port \$PORT
# Step 3
# web: DATABASE_URL=${DATABASE_URL/postgres/postgresql} airflow webserver --port \$PORT --daemon & airflow scheduler
